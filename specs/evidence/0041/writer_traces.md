# 0041 treatment matrix — the nine writer traces (round-2 F6; research's rulings left these OWED)

*Dev, 2026-09-15, at the round-2 pin's src. The question research put: for each bare-`str` field with no validator, is every WRITER internal (the product's own vocabulary, a literal or a module constant) or can a host, an extractor or an import file set it? Traced by a write-site sweep of `src/veracium` (`field=` keyword writes, dict-key writes and parameters), each site classified; one field needed execution and got it.*

| field (matrix row) | write sites in `src/veracium` | writer | trace |
|---|---|---|---|
| `ConsolidationOp.owner` (row 12) | `lifecycle.py:216` `owner = f"consolidate:{uuid…}"`; the Store methods `create_or_takeover_consolidation`, `renew_consolidation_lease`, `write_consolidation_output_if_current`, `transition_consolidation_if_current` take `owner` as a parameter (`sqlite.py:1930/1973/1986/2067`) | INTERNAL at the product API (the only product caller is `lifecycle.py`, product-generated); the Store methods are a public class surface, so a host driving the Store directly can pass any string — the model docstring says "cooperative identity, not an auth capability". Not exported, not imported (consolidation_ops never travel). | sweep |
| `ContributionRecord.contributor_type` (rows ×2: the record and the plan draft) | `graph.py:515` literal `"edge"`; `scope_linkage.py:354` literal `"edge"` (the import-side reconstruction); `sqlite.py:1442` read-back | INTERNAL — every writer is the literal `"edge"`; import RECONSTRUCTS the row (it does not copy the file's) | sweep |
| `ContributionRecord.site` (rows ×2) | `graph.py:514` `"absorption"`; `scope.py:560` `SITE_ATTRIBUTION`; `scope_linkage.py:474` chooses `SITE_IMPORTED` / `SITE_ATTRIBUTION`; `sqlite.py:1334` `SITE_ATTRIBUTION`; and `contribution.validate_payload(site, …)` REFUSES a site outside `SITES` ("the site set is CLOSED … 0014 §4a") | INTERNAL and CLOSED at the write path — constants only, and the validator refuses an unknown site; `_is_canonical` reads `row.get("site")` but from the product's own rows | sweep + the validator's own text |
| `ContributionRecord.survivor_type` (rows ×2) | `graph.py:514` `"edge"`; `portability.py:638` `"edge"` (import reconstruction); reads elsewhere | INTERNAL — the literal `"edge"` at every writer | sweep |
| `EdgeEvent.kind` (row: `edge_event.kind`) | `sqlite.py:241/245` `"created"` / `"mutated"`; `:646` `"invalidated"`; `:698` `"reinstated"`; `edge_events.py:76` `"baseline"`; `asof/resolve.py:284` `"live"` (a synthetic in-memory event, not written) | INTERNAL — five literal kinds at the writers; no import path carries journal events today (round-2 §4g: export carries none) | sweep |
| `Episode.kind` (row: `Episode.kind`) | `sqlite.py:1652` `kind="outcome"`; `__init__.py:2018` `kind="corrected"`; the model default `"interaction"` (`schema.py:826`, comment "interaction \| outcome", NO validator); **and the import path**: an export record's `kind` is read from the file | 🔴 **EXTERNAL through import** — EXECUTED (`kind_import_probe.py`): an export record with `kind = "told me in confidence: hiv-positive"` is ACCEPTED by `import_memory` and stored verbatim (`episodes: 1`, stored kind == the prose). The product's own writers are literals; the file is not. | executed |
| `SupersessionRefusal.rule_version` (row) | `sqlite.py:1136` writes the module constant `RULE_VERSION` (from `authority.py`); `graph.py:311` carries it into an exception; `sqlite.py:1187` read-back | INTERNAL — one constant; refusals are not imported | sweep |

**So:** eight of the nine are INTERNAL by every writer in the tree (four of them literal `"edge"`, `site` closed by a validator, `kind` five literals, `rule_version` one constant, `owner` product-generated at the API with the caveat that the Store class surface accepts a string). **One is EXTERNAL: `Episode.kind` reaches disk from an import file unvalidated** — the same class as `retired_reason` in round 1 (F1b) and `outcome_counts` in round 2 (F6): the model's comment names a closed set the model does not enforce. Research's rule then gives: PRESERVE for the eight; `Episode.kind` either REPLACE, or PRESERVE once import (and the model) close it to `{"interaction", "outcome", "corrected"}` — a write-path closure this spec would have to name as an amendment to 0006/portability, which is the honest form ("we only ever write safe values" is exactly the claim the trace refutes for this field).
