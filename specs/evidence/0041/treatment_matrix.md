# 0041 — the field-treatment matrix (round-2 F6): one row per carrier the enumeration derived

*Skeleton generated by dev on 2026-09-15 from `carrier_enumeration_OUTPUT.txt` (the 64 carriers, in the enumeration's order); the `dev proposal` column is a PROPOSAL by simple rules on the field's role, the `ruling` column is research's to fill (the contract), and `resulting shape` must be a VALID post-redaction value under the model's validators. Treatment vocabulary (the reviewer's): REPLACE (the marker), CLEAR (empty / removed keys), DELETE (the row), PRESERVE (untouched), EXCLUDE (explicitly outside redaction, with the reason). The set is mandatory and independent of what an implementation lists in its receipt.*

| # | candidate | where it reaches disk | dev proposal | ruling | resulting shape |
|---|---|---|---|---|---|
| 1 | `AgreementRecord.direction` | nested in Edge (json blob) | PRESERVE (closed set — confirm the set is closed at the write path, else REPLACE) | @@ | @@ |
| 2 | `AgreementRecord.lexicon` | nested in Edge (json blob) | PRESERVE (closed set — confirm the set is closed at the write path, else REPLACE) | @@ | @@ |
| 3 | `AgreementRecord.markers` | nested in Edge (json blob) | REPLACE (per entry; the uniqueness validator must admit repeated markers or the list is CLEARED — research rules) | @@ | @@ |
| 4 | `Confirmation.correlation_id` | RULED carrier -> confirmations | PRESERVE (identifier / bounded) | @@ | @@ |
| 5 | `Confirmation.edge_id` | RULED carrier -> confirmations | PRESERVE (identifier / bounded) | @@ | @@ |
| 6 | `Confirmation.id` | RULED carrier -> confirmations | PRESERVE (identifier / bounded) | @@ | @@ |
| 7 | `Confirmation.request_digest` | RULED carrier -> confirmations | DELETE the row (INV-7) / never copied into a receipt | @@ | @@ |
| 8 | `Confirmation.user_id` | RULED carrier -> confirmations | PRESERVE (identifier / bounded) | @@ | @@ |
| 9 | `ConsolidationOp.claimed_ids` | IS the row -> consolidation_ops | REPLACE (marker) | @@ | @@ |
| 10 | `ConsolidationOp.lease_expires_at` | IS the row -> consolidation_ops | PRESERVE (timestamp) | @@ | @@ |
| 11 | `ConsolidationOp.operation_id` | IS the row -> consolidation_ops | PRESERVE (identifier / bounded) | @@ | @@ |
| 12 | `ConsolidationOp.owner` | IS the row -> consolidation_ops | PRESERVE (closed set — confirm the set is closed at the write path, else REPLACE) | @@ | @@ |
| 13 | `ConsolidationOp.user_id` | IS the row -> consolidation_ops | PRESERVE (identifier / bounded) | @@ | @@ |
| 14 | `ContributionDraft.contributor_id` | RULED carrier -> contribution_ledger | PRESERVE (identifier / bounded) | @@ | @@ |
| 15 | `ContributionDraft.contributor_type` | RULED carrier -> contribution_ledger | PRESERVE (closed set — confirm the set is closed at the write path, else REPLACE) | @@ | @@ |
| 16 | `ContributionDraft.site` | RULED carrier -> contribution_ledger | PRESERVE (closed set — confirm the set is closed at the write path, else REPLACE) | @@ | @@ |
| 17 | `ContributionDraft.survivor_id` | RULED carrier -> contribution_ledger | PRESERVE (identifier / bounded) | @@ | @@ |
| 18 | `ContributionDraft.survivor_type` | RULED carrier -> contribution_ledger | PRESERVE (closed set — confirm the set is closed at the write path, else REPLACE) | @@ | @@ |
| 19 | `ContributionRecord.contributor_ref` | IS the row -> contribution_ledger | PRESERVE (identifier / bounded) | @@ | @@ |
| 20 | `ContributionRecord.contributor_type` | IS the row -> contribution_ledger | PRESERVE (closed set — confirm the set is closed at the write path, else REPLACE) | @@ | @@ |
| 21 | `ContributionRecord.evidence_ref_digest` | IS the row -> contribution_ledger | DELETE the row (INV-7) / never copied into a receipt | @@ | @@ |
| 22 | `ContributionRecord.id` | IS the row -> contribution_ledger | PRESERVE (identifier / bounded) | @@ | @@ |
| 23 | `ContributionRecord.identity_digest` | IS the row -> contribution_ledger | DELETE the row (INV-7) / never copied into a receipt | @@ | @@ |
| 24 | `ContributionRecord.op_key` | IS the row -> contribution_ledger | PRESERVE (identifier / bounded) | @@ | @@ |
| 25 | `ContributionRecord.site` | IS the row -> contribution_ledger | PRESERVE (closed set — confirm the set is closed at the write path, else REPLACE) | @@ | @@ |
| 26 | `ContributionRecord.survivor_id` | IS the row -> contribution_ledger | PRESERVE (identifier / bounded) | @@ | @@ |
| 27 | `ContributionRecord.survivor_type` | IS the row -> contribution_ledger | PRESERVE (closed set — confirm the set is closed at the write path, else REPLACE) | @@ | @@ |
| 28 | `ContributionRecord.user_id` | IS the row -> contribution_ledger | PRESERVE (identifier / bounded) | @@ | @@ |
| 29 | `Edge.id` | json blob | PRESERVE (identifier / bounded) | @@ | @@ |
| 30 | `Edge.invalidation_reason` | json blob | PRESERVE if vocabulary; REPLACE if legacy prose (§11.2's boundary) | @@ | @@ |
| 31 | `Edge.note` | json blob | REPLACE (marker) | @@ | @@ |
| 32 | `Edge.object` | json blob | REPLACE (marker) | @@ | @@ |
| 33 | `Edge.original_relation` | json blob | REPLACE (marker) | @@ | @@ |
| 34 | `Edge.outcome_counts` | json blob | CLEAR the keys (a prose key is content; the Outcome enum bounds record_outcome only) | @@ | @@ |
| 35 | `Edge.relation` | json blob | REPLACE (marker) | @@ | @@ |
| 36 | `Edge.subject` | json blob | REPLACE (marker) | @@ | @@ |
| 37 | `Edge.supersedes` | json blob | PRESERVE (identifier / bounded) | @@ | @@ |
| 38 | `Edge.user_id` | json blob | PRESERVE (identifier / bounded) | @@ | @@ |
| 39 | `Episode.claimed_by` | json blob | PRESERVE (identifier / bounded) | @@ | @@ |
| 40 | `Episode.context_ref` | json blob | PRESERVE (identifier / bounded) | @@ | @@ |
| 41 | `Episode.date` | json blob | PRESERVE (timestamp) | @@ | @@ |
| 42 | `Episode.date_end` | json blob | PRESERVE (timestamp) | @@ | @@ |
| 43 | `Episode.date_start` | json blob | PRESERVE (timestamp) | @@ | @@ |
| 44 | `Episode.edge_id` | json blob | PRESERVE (identifier / bounded) | @@ | @@ |
| 45 | `Episode.id` | json blob | PRESERVE (identifier / bounded) | @@ | @@ |
| 46 | `Episode.kind` | json blob | PRESERVE (closed set — confirm the set is closed at the write path, else REPLACE) | @@ | @@ |
| 47 | `Episode.lineage` | json blob | PRESERVE (identifier / bounded) | @@ | @@ |
| 48 | `Episode.operation_id` | json blob | PRESERVE (identifier / bounded) | @@ | @@ |
| 49 | `Episode.retired_reason` | json blob | PRESERVE if vocabulary; REPLACE if legacy prose (§11.2's boundary) | @@ | @@ |
| 50 | `Episode.summary` | json blob | REPLACE (marker) | @@ | @@ |
| 51 | `Episode.supersedes_episode` | json blob | PRESERVE (identifier / bounded) | @@ | @@ |
| 52 | `Episode.user_id` | json blob | PRESERVE (identifier / bounded) | @@ | @@ |
| 53 | `Provenance.evidence_ref` | nested in Edge (json blob) | §2 EXCLUDES (the host's copy is the host's obligation) — RULE the example's tombstone against it | @@ | @@ |
| 54 | `Provenance.origin` | nested in Edge (json blob) | PRESERVE (identifier / bounded) | @@ | @@ |
| 55 | `Provenance.source_id` | nested in Edge (json blob) | PRESERVE (identifier / bounded) | @@ | @@ |
| 56 | `SupersessionRefusal.incoming_edge_id` | IS the row -> supersession_refusals | PRESERVE (identifier / bounded) | @@ | @@ |
| 57 | `SupersessionRefusal.prior_edge_id` | IS the row -> supersession_refusals | PRESERVE (identifier / bounded) | @@ | @@ |
| 58 | `SupersessionRefusal.refusal_id` | IS the row -> supersession_refusals | PRESERVE (identifier / bounded) | @@ | @@ |
| 59 | `SupersessionRefusal.relation` | IS the row -> supersession_refusals | REPLACE (marker) | @@ | @@ |
| 60 | `SupersessionRefusal.rule_version` | IS the row -> supersession_refusals | PRESERVE (closed set — confirm the set is closed at the write path, else REPLACE) | @@ | @@ |
| 61 | `SupersessionRefusal.user_id` | IS the row -> supersession_refusals | PRESERVE (identifier / bounded) | @@ | @@ |
| 62 | `SupersessionRefusalDraft.incoming_edge_id` | IS the row -> supersession_refusals | PRESERVE (identifier / bounded) | @@ | @@ |
| 63 | `SupersessionRefusalDraft.prior_edge_id` | IS the row -> supersession_refusals | PRESERVE (identifier / bounded) | @@ | @@ |
| 64 | `SupersessionRefusalDraft.relation` | IS the row -> supersession_refusals | REPLACE (marker) | @@ | @@ |

*Non-carriers (20) are listed in the enumeration output with the reason each cannot carry text; they need no row here. `Edge.outcome_counts` is a carrier by round-2 F6 (keys persist and export verbatim). The three duplicated columns (`edges.subject/relation/object`) follow their json field's row (INV-2).*
